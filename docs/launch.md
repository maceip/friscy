# Launch Checklists & Tasks

## Current UI & Fix Tasks
- [ ] 1. Increase the size of `mcp.svg` and `riscv.svg` to 2x.
- [ ] 2. Make the Go (`golang.svg`) tab icon the exact same size as the Node.js and Alpine icons.
- [ ] 3. Move the network status (`net: off` and other colored messages) to the bottom-right corner of the screen.
- [ ] 4. Move the `mcp.svg` and `riscv.svg` logos to the bottom-middle of the screen.
- [ ] 5. Merge the suspend and resume options into a single, cohesive button and place it at the same vertical height as the tabs, anchored to the far right.
- [ ] 6. Increase the size of `friscylogo.png` by 3x and place it in the bottom-left corner. Add a bounce animation on hover and a dodge animation on click.
- [ ] 7. Identify and fix the root cause of the VM getting stuck indefinitely on "Preparing...".
- [ ] 8. Update the loading text to accurately explain what's happening during boot instead of just "Preparing...", and remove the "friscy" header from the terminal loading view.
